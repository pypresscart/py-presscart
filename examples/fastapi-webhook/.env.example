# Signing secret from the Presscart webhook detail page (Reveal -> copy).
PRESSCART_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxx

# Optional: a read-scoped API token used to hydrate full objects after a
# delivery (e.g. fetch the article on article.status_changed). The receiver
# runs fine without it.
PRESSCART_API_TOKEN=
