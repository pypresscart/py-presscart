fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1

# Optional: only needed for the article-enrichment path in app.py
# (_maybe_enrich_article). The receiver itself has no runtime deps beyond
# FastAPI + uvicorn.
pypresscart>=0.1.7
